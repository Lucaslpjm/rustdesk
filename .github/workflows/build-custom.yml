name: Build Saurus Installer (Generator Fix)

on:
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-2022
    
    env:
      LIBCLANG_PATH: "C:\\Program Files\\LLVM\\bin"
      RUSTDESK_API_SERVER: "" 
      VCPKG_BINARY_SOURCES: "clear;default,readwrite"

    steps:
    - name: Checkout source code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    # --- CACHES ---
    - name: Cache Rust Dependencies
      uses: swatinem/rust-cache@v2
      with:
        workspaces: .

    - name: Cache VCPKG Archives
      uses: actions/cache@v4
      with:
        path: C:\Users\runneradmin\AppData\Local\vcpkg\archives
        key: vcpkg-archives-${{ runner.os }}-${{ hashFiles('vcpkg.json') }}
        restore-keys: |
          vcpkg-archives-${{ runner.os }}-

    - name: Cache Flutter Packages
      uses: actions/cache@v4
      with:
        path: |
          flutter/.pub-cache
          flutter/.dart_tool
        key: flutter-pub-${{ runner.os }}-${{ hashFiles('flutter/pubspec.yaml') }}
        restore-keys: |
          flutter-pub-${{ runner.os }}-

    # --- SETUP ---
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.22.2'
        channel: 'stable'

    - name: Install LLVM & Inno Setup
      run: |
        choco install llvm -y
        choco install innosetup -y
        echo "C:\Program Files\LLVM\bin" >> $env:GITHUB_PATH
      shell: powershell

    # --- CONFIGURAÇÃO DO SERVIDOR ---
    - name: Inject Server Configuration
      shell: powershell
      run: |
        Write-Host "Configurando servidor..."
        $ConfigFile = "libs/hbb_common/src/config.rs"
        
        # ID na 80, Relay na 443, Key com =
        $SeuIP = "20.195.216.23:80" 
        $SuaChave = "OJ7QiUrqNu0wM13vDSp4nmAlDu6hy3n8hTI5Wksl2Tc=" 
        
        if (Test-Path $ConfigFile) {
           $content = Get-Content $ConfigFile -Raw
           $content = $content -replace '(?s)pub const RENDEZVOUS_SERVERS:.*?;', "pub const RENDEZVOUS_SERVERS: &'static [&'static str] = &[`"$SeuIP`"];"
           $content = $content -replace '(?s)pub const RS_PUB_KEY:.*?;', "pub const RS_PUB_KEY: &'static str = `"$SuaChave`";"
           Set-Content -Path $ConfigFile -Value $content -Encoding UTF8
        } else { exit 1 }

    # --- COMPILAÇÃO ---
    - name: Fix Flutter Dependencies
      working-directory: ./flutter
      shell: powershell
      run: |
        $File = "pubspec.yaml"
        $Content = Get-Content $File -Raw
        $NewContent = $Content -replace 'extended_text:.*', 'extended_text: ^13.0.0'
        Set-Content -Path $File -Value $NewContent -Encoding UTF8

    - name: Prepare Flutter Dependencies
      working-directory: ./flutter
      run: flutter pub get
      shell: cmd

    - name: Install Flutter Rust Bridge Codegen
      run: |
        cargo install flutter_rust_bridge_codegen --version 1.80.1 --features uuid
      shell: powershell

    - name: Generate Bridge Code
      shell: powershell
      run: |
        $env:PATH += ";C:\Users\runneradmin\.cargo\bin"
        flutter_rust_bridge_codegen --rust-input src/flutter_ffi.rs --dart-output flutter/lib/generated_bridge.dart --rust-output src/bridge_generated.rs

    - name: Install C++ Libs (VCPKG)
      shell: powershell
      run: |
        vcpkg install --triplet x64-windows-static
        $BaseDir = Get-Location
        $IncludePath = Join-Path $BaseDir "vcpkg_installed\x64-windows-static\include"
        $LibPath = Join-Path $BaseDir "vcpkg_installed\x64-windows-static\lib"
        echo "BINDGEN_EXTRA_CLANG_ARGS=-I$IncludePath" >> $env:GITHUB_ENV
        echo "CPATH=$IncludePath" >> $env:GITHUB_ENV
        echo "RUSTFLAGS=-L native=$LibPath" >> $env:GITHUB_ENV
        echo "VCPKG_ROOT=$BaseDir" >> $env:GITHUB_ENV

    - name: Build RustDesk
      run: python build.py --flutter
      env:
        VCPKG_DEFAULT_TRIPLET: "x64-windows-static"
    
    # --- CRIAÇÃO DO INSTALADOR (SCRIPT GERADO NA HORA) ---
    - name: Create and Run Installer
      shell: powershell
      run: |
        Write-Host "Preparando arquivos..."
        New-Item -ItemType Directory -Force -Path "target\release"
        
        # 1. Copia os arquivos compilados para a pasta de stage
        $Files = Get-ChildItem -Path . -Recurse -Filter "rustdesk.exe" | Where-Object { $_.FullName -notmatch "vcpkg" } | Select-Object -First 1
        $SourceFolder = $Files.Directory.FullName
        Copy-Item -Path "$SourceFolder\*" -Destination "target\release" -Recurse -Force
        
        # 2. CRIA O ARQUIVO .ISS MANUALMENTE (Aqui está a mágica)
        $IssContent = @"
        [Setup]
        AppName=Saurus Remote
        AppVersion=1.2.4
        DefaultDirName={autopf}\Saurus Remote
        DefaultGroupName=Saurus Remote
        UninstallDisplayIcon={app}\rustdesk.exe
        Compression=lzma2
        SolidCompression=yes
        OutputDir=.
        OutputBaseFilename=Saurus_Instalador
        PrivilegesRequired=lowest

        [Files]
        ; Pega tudo da pasta que preparamos e coloca no instalador
        Source: "target\release\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

        [Icons]
        ; Cria atalho no Menu Iniciar e Desktop
        Name: "{group}\Saurus Remote"; Filename: "{app}\rustdesk.exe"
        Name: "{autodesktop}\Saurus Remote"; Filename: "{app}\rustdesk.exe"; Tasks: desktopicon

        [Tasks]
        Name: "desktopicon"; Description: "Criar atalho na Area de Trabalho"; GroupDescription: "Atalhos:"; Flags: unchecked
        "@
        
        # Salva o arquivo .iss no disco
        Set-Content -Path "saurus_setup.iss" -Value $IssContent -Encoding ASCII
        
        # 3. Roda o compilador Inno Setup
        Write-Host "Compilando o Instalador..."
        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" saurus_setup.iss

    - name: Upload Single Installer
      uses: actions/upload-artifact@v4
      with:
        name: Saurus-Setup
        path: Saurus_Instalador.exe
